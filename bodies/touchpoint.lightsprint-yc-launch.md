# Lightsprint YC Launch

## Modeling note

This object is kept only as a historical modeling artifact. YC Launch is a one-time content item and should be modeled as `source.item`, not as a persistent `touchpoint`. The canonical source object is `source.yc-launch.lightsprint`; future YC launch pages should not create touchpoint records.
