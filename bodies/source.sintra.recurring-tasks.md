# Recurring scheduled Helper tasks

官方文档说明用户可把 Helper 任务设为每天、每周或其他周期执行，结果进入 Inbox；任务依赖 workspace credits 和有效 integration。删除或失效的连接会导致任务失败。

**可支持：** Sintra 已具备后台定时执行和结果收件箱，而不只是一问一答聊天。

**边界：** 定时存在不等于任务成功、结果被采纳或形成完整业务闭环；默认结果主要留在产品内。
