# Anyway SuperAPI 文档

URL: https://docs.anyway.sh/features/super-api

SuperAPI 以 USDC on Base 按调用付费。调用方先收到 x402 402 challenge，再用 Agent Wallet 或私钥签 EIP-3009 authorization，重试后获得 API 结果。

文档列出的供给覆盖 AI、search、crypto、social、finance、browser、security、travel、legal 等。实时价格由 402 challenge 返回，不应把文档示例价格当长期固定价。
