# Anyway Docs / llms.txt

URL: https://docs.anyway.sh/llms.txt

文档索引覆盖 Merchant API、billing、Agent Wallet、cost tracking、customer/order attribution、payments、SuperAPI、distributed tracing、web app、webhooks、CLI、JavaScript/Python SDK。

Quickstart 让本地 Agent 创建 USDC on Base 钱包，或创建收款链接。文档是判断公开产品面最可靠的一手入口。
