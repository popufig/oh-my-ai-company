# Arga Agent Security Testing Docs

## Modeling note

This object is kept only as a historical modeling artifact. A single docs page should be modeled as `source.item`, not a persistent `touchpoint`. The canonical source object is `source.docs.arga-agent-security`; future docs/articles should attach to the website/docs feed touchpoint when one exists.
