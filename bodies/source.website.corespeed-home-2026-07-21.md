2026-07-21 真实读取 CoreSpeed 官网。首屏定位是“Your AI agent is smart. Now give it access.”，支持 Claude Code、Codex、Cursor、OpenClaw、Hermes 与 custom agent。产品面展示 connectors、built-in tools、smart approvals、memory、Agent Pay、One Bill，并声称连接 1,000+ apps、一次 OAuth 授权、policy server-side enforcement、跨 Agent 保留 apps/memory/voice/policy。

本轮点击所有主要 CTA，公开入口均为 `Request early access` 邮箱表单，没有账号、控制台、connector directory、API/docs 或 pricing。官网 UI 中的付款和账单数字为动画/占位演示，不能作为交易、客户或收入证据。该 source 同时承载产品设计与公开 availability 边界，关于 [[company.corespeed]]。
