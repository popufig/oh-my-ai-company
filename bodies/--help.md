# Accidental CLI Artifact

This object was created accidentally when probing `mmx source add --help`; the CLI interpreted `--help` as a source id. It is retained only because no safe delete command was available during the research run.
